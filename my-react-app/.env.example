# Backend API base URL
VITE_BACKEND_URL=http://localhost:3001

# Google OAuth client ID (public — same Web client ID as backend GOOGLE_CLIENT_ID)
# Setup: docs/identity/google-oauth-setup.md
VITE_GOOGLE_CLIENT_ID=
